import 'package:flutter/material.dart';

class SubMenuDialog extends StatelessWidget {
  const SubMenuDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
