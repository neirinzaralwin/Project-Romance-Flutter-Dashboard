import 'package:flutter/material.dart';

class LayoutState {
  final int? currentSubMenuIndex;
  final Widget? mainScreenWidget;
  const LayoutState({this.currentSubMenuIndex, this.mainScreenWidget});
}
